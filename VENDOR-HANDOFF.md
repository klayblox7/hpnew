# 여유톡 메인 리뉴얼 전달 문서

## 전달 파일

- `index.html`: 새 메인 홈페이지 정적 시안입니다. CSS와 간단한 지역 필터 스크립트가 한 파일 안에 들어 있습니다.
- `assets/hero-moon-ost.png`: GPT 이미지 생성으로 만든 새 메인 배너 이미지입니다. 이미지 안에는 텍스트, 로고, 상표 요소가 없습니다.
- `assets/events/`: 2026-05-08 기준 기존 홈페이지에서 사용 중인 행사 썸네일을 로컬로 저장한 폴더입니다.
- `verification-desktop.png`, `verification-mobile.png`: 데스크톱/모바일 렌더링 확인용 스크린샷입니다.
- `verification-report.json`: 렌더링 점검 결과입니다.

## 작업 목적

현재 `https://yeoyoutalk.kr/` 메인을 첨부 디자인처럼 보이도록 먼저 구성한 정적 HTML입니다.  
업체에서는 이 파일을 기준으로 기존 ASP 관리자, 행사 등록/삭제, 문자발송, 행사 리스트 출력 기능과 연결하면 됩니다.

## 업체 연동 요청 범위

1. `index.html`의 정적 행사 카드 영역을 기존 관리자 DB 기반 반복 출력으로 교체해 주세요.
2. 각 행사 카드에는 아래 값이 들어가면 됩니다.
   - 행사 번호: 상세 페이지 링크 `event.asp?idx=행사번호`
   - 썸네일 이미지
   - 지역명
   - 날짜/요일
   - 장소명
3. 카드의 `data-region` 값은 지역 필터에 사용됩니다.
   - 예: `<article class="event-card" data-region="경기">`
4. 기존 관리자에서 행사 등록/삭제를 하면 메인 카드 목록도 자동 반영되도록 처리해 주세요.
5. 문자발송, 신청자 목록, 행사 신청 처리 기능은 기존 관리자 기능을 유지하거나 기존 구조에 맞게 연결해 주세요.
6. 현재 `index.html`의 신청 버튼은 각 행사 상세 페이지로 이동하는 링크입니다. 기존 신청 폼이 상세 페이지에 있다면 그대로 사용하면 됩니다.

## 디자인/퍼블리싱 메모

- 메인 배너 이미지는 배경으로만 사용하고, 제목/설명/버튼은 HTML 텍스트로 유지했습니다. 나중에 문구 수정이나 SEO 관리가 쉽습니다.
- CSS는 현재 `index.html` 안에 포함되어 있습니다. 운영 반영 시 `/css/main.css` 같은 파일로 분리해도 됩니다.
- JavaScript는 지역 필터만 담당합니다. 운영 DB 출력 후에도 `data-region`만 맞으면 그대로 작동합니다.
- 모바일 기준 가로 스크롤이 생기지 않도록 확인했습니다.
- 기존 홈페이지의 행사 이미지는 일부 저작권성 캐릭터가 포함되어 보입니다. 운영상 사용 권리는 홈페이지 보유자와 업체가 최종 확인해 주세요.

## 확인 결과

2026-05-08에 Playwright로 확인했습니다.

- 데스크톱 1180px: 가로 넘침 없음, 행사 이미지 14개 정상 로딩
- 모바일 390px: 가로 넘침 없음, 행사 이미지 14개 정상 로딩
- 지역 필터: 경기 선택 시 5개 카드 노출 확인

## 반영 방식 제안

기존 ASP 사이트라면 아래 순서가 가장 안전합니다.

1. 현재 메인 파일을 백업합니다.
2. `index.html`의 `<style>` 내용을 기존 공통 CSS 또는 새 CSS 파일로 옮깁니다.
3. `<body>` 안의 구조를 기존 메인 템플릿에 적용합니다.
4. 행사 카드 HTML 부분만 DB 반복문으로 교체합니다.
5. 이미지 경로를 기존 `/upload/mainfile/...` 경로 또는 새 업로드 경로에 맞춥니다.
6. 운영 서버에서 행사 등록, 삭제, 상세 링크, 신청 버튼, 문자발송 흐름을 테스트합니다.
